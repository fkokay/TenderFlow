@{
    ViewData["title"] = "Sevkiyat Emri Yönetimi";
}

<h4 class="fw-bold mb-4 py-3">
    <span class="text-muted fw-light">@ViewData["title"]</span>
</h4>

<div class="card mb-4">
    <div class="card-header pb-1">
        <h5 class="mb-0">Filtreler</h5>
    </div>
    <div class="card-body pb-2">
        <div class="row g-3">

            <!-- Başlangıç Tarihi -->
            <div class="col-md-3">
                <label class="form-label">Başlangıç Tarihi</label>
                <input type="date" id="startDate" class="form-control">
            </div>

            <!-- Bitiş Tarihi -->
            <div class="col-md-3">
                <label class="form-label">Bitiş Tarihi</label>
                <input type="date" id="endDate" class="form-control">
            </div>

            <!-- Durum -->
            <div class="col-md-3">
                <label class="form-label">Durum</label>
                <select id="status" class="form-select">
                    <option value="0">Tümü</option>
                    <option value="1">Beklemede</option>
                    <option value="2">Toplamada</option>
                    <option value="3">Hazır</option>
                    <option value="4">Tamamlandı</option>
                </select>
            </div>

            <!-- Tamamlananları Göster -->
            <div class="col-md-3 d-flex align-items-end">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="showCompleted">
                    <label class="form-check-label" for="showCompleted">
                        Tamamlanan Kayıtları Göster
                    </label>
                </div>
            </div>

            <!-- Sıfır Fiyatlıları Renklendir -->
            <div class="col-md-3 d-flex align-items-end">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="highlightZeroPrice">
                    <label class="form-check-label" for="highlightZeroPrice">
                        Sıfır Fiyatlıları Renklendir
                    </label>
                </div>
            </div>
        </div>
        <div class="card-footer text-end">
            <button type="button" class="btn btn-primary" id="btnFilter">Filtrele</button>
            <button type="button" class="btn btn-secondary" id="btnClearFilters">Temizle</button>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <button class="btn btn-primary" onclick="createDocument()">İrsaliye Oluştur</button>
    </div>
    <div class="card-datatable text-nowrap pt-0">
        <div class="table-loading" id="tableLoading" style="display:none;">
            <div class="spinner-border text-light" style="width:3rem; height:3rem;"></div>
            <div class="mt-2 text-white">Yükleniyor...</div>
        </div>
        <table id="Table" class="table-bordered table">
            <thead>
                <tr>
                    <th></th>
                    <th>Sevk Emri No.</th>
                    <th>Sipariş No</th>
                    <th>Tarih</th>
                    <th>Cari Kodu</th>
                    <th>Cari Adı</th>
                    <th>E-Fatura Carisi</th>
                    <th>Durum</th>
                    <th>Kullanıcı</th>
                    <th>Kapalı</th>
                    <th>Kısmi Teslimat</th>
                    <th>Miktar</th>
                    <th>Toplanan</th>
                    <th>Kalan</th>
                    <th>F/I Edilen</th>
                    <th>F/I Edilemeyen</th>
                </tr>
            </thead>
        </table>
    </div>
</div>

@section VendorStyles {
    <link href="~/vendor/libs/datatables-bs5/datatables.bootstrap5.css" rel="stylesheet" />
    <link href="~/vendor/libs/datatables-responsive-bs5/responsive.bootstrap5.css" rel="stylesheet" />
    <link href="~/vendor/libs/datatables-select-bs5/select.bootstrap5.css" rel="stylesheet" />
    <link href="~/vendor/libs/datatables-checkboxes-jquery/datatables.checkboxes.css" rel="stylesheet" />
    <link href="~/vendor/libs/datatables-fixedcolumns-bs5/fixedcolumns.bootstrap5.css" rel="stylesheet" />
    <link href="~/vendor/libs/datatables-fixedheader-bs5/fixedheader.bootstrap5.css" rel="stylesheet" />
    <link href="~/vendor/libs/datatables-buttons-bs5/buttons.bootstrap5.css" rel="stylesheet" />
}

@section VendorScripts {
    <script src="~/vendor/libs/datatables-bs5/datatables-bootstrap5.js"></script>
    <script src="~/vendor/libs/moment/moment.js"></script>
}

@section PageScripts {
    <script>
        const table = $('#Table').DataTable({
            language: {
                paginate: {
                    next: '<i class="icon-base ti ti-chevron-right scaleX-n1-rtl icon-18px"></i>',
                    previous: '<i class="icon-base ti ti-chevron-left scaleX-n1-rtl icon-18px"></i>',
                    first: '<i class="icon-base ti ti-chevrons-left scaleX-n1-rtl icon-18px"></i>',
                    last: '<i class="icon-base ti ti-chevrons-right scaleX-n1-rtl icon-18px"></i>'
                },
                url: 'https://cdn.datatables.net/plug-ins/2.3.2/i18n/tr.json',
            },
            processing: false,
            serverSide: true,
            paging: false,
            searching: false,
            order: [
                [0, 'asc']
            ],
            ajax: {
                url: '/Shipment/ShipmentOrderManagementList',
                type: 'POST',
                contentType: 'application/json',
                data: function(d) {
                    d.Filters = {
                        startDate: $("#startDate").val(),
                        endDate: $("#endDate").val(),
                        status: $("#status").val(),
                        showCompleted: $("#showCompleted").is(":checked"),
                        highlightZeroPrice: $("#highlightZeroPrice").is(":checked")
                    };
                    return JSON.stringify(d);
                },
            },
            deferLoading: 0,
            columns: [{
                    data: null,
                    orderable: false,
                    searchable: false,
                    render: function() {
                        return '<input type="checkbox" class="row-checkbox"/>';
                    }
                },
                {
                    data: 'BELGE_NO'
                },
                {
                    data: 'SIPARIS_NO'
                },
                {
                    data: 'TARIH',
                    render: function(data, type) {
                        if (type === "sort" || type === "type") return data;
                        return moment(data).format("DD.MM.YYYY");
                    }
                },
                {
                    data: 'CARI_KODU'
                },
                {
                    data: 'CARI_ADI'
                },
                {
                    data: 'EFATURA_CARISI'
                },
                {
                    data: 'DURUM_ACIKLAMA'
                },
                {
                    data: 'KULLANICI_ADSOYAD'
                },
                {
                    data: 'KAPALI'
                },
                {
                    data: 'KISMI_TESLIMAT'
                },
                {
                    data: 'TOPLAM_MIKTAR'
                },
                {
                    data: 'TOPLAM_TOPLANAN'
                },
                {
                    data: 'TOPLAM_KALAN'
                },
                {
                    data: 'TOPLAM_IRS_EDILEN'
                },
                {
                    data: 'TOPLAM_IRS_EDILMEYEN'
                }
            ],
            displayLength: 10,
            layout: {
                topStart: {
                    rowClass: "card-header d-flex border-top rounded-0 flex-wrap py-0 flex-column flex-md-row align-items-start",
                    features: [{
                        search: {
                            className: "me-5 ms-n4 pe-5 mb-n6 mb-md-0",
                            placeholder: "Ara..",
                            text: "_INPUT_"
                        }
                    }]
                },
                topEnd: {
                    rowClass: "row m-3 my-0 justify-content-between",
                    features: [{
                        pageLength: {
                            menu: [10, 25, 50, 100],
                            text: "_MENU_"
                        },
                        buttons: [{
                                extend: "collection",
                                className: "btn btn-label-secondary dropdown-toggle me-4",
                                text: `<span class="d-flex align-items-center gap-1">
                                            <i class="icon-base ti ti-upload icon-xs"></i>
                                            <span class="d-none d-sm-inline-block">Dışarı Aktar</span>
                                          </span>`,
                                buttons: [{
                                        extend: "print",
                                        className: "dropdown-item",
                                        text: `<span class="d-flex align-items-center"><i class="icon-base ti tabler-printer me-1"></i> Print</span>`,
                                        exportOptions: {
                                            columns: [2, 3, 4]
                                        }
                                    },
                                    {
                                        extend: "csv",
                                        className: "dropdown-item",
                                        text: `<span class="d-flex align-items-center"><i class="icon-base ti tabler-file me-1"></i> Csv</span>`,
                                        exportOptions: {
                                            columns: [2, 3, 4]
                                        }
                                    },
                                    {
                                        extend: "excel",
                                        className: "dropdown-item",
                                        text: `<span class="d-flex align-items-center"><i class="icon-base ti tabler-upload me-1"></i> Excel</span>`,
                                        exportOptions: {
                                            columns: [2, 3, 4]
                                        }
                                    },
                                    {
                                        extend: "pdf",
                                        className: "dropdown-item",
                                        text: `<span class="d-flex align-items-center"><i class="icon-base ti tabler-file-text me-1"></i> Pdf</span>`,
                                        exportOptions: {
                                            columns: [2, 3, 4]
                                        }
                                    },
                                    {
                                        extend: "copy",
                                        className: "dropdown-item",
                                        text: `<i class="icon-base ti tabler-copy me-1"></i> Copy`,
                                        exportOptions: {
                                            columns: [2, 3, 4]
                                        }
                                    }
                                ]
                            },
                            {
                                text: `<i class="icon-base ti ti-plus me-sm-1 icon-16px me-0"></i>
                                            <span class="d-none d-sm-inline-block">Yeni Ekle</span>`,
                                className: "add-new btn btn-primary",
                                action: function() {
                                    window.location.href = "Create";
                                }
                            }
                        ]
                    }]
                },
                bottomStart: {
                    rowClass: "row mx-3 justify-content-between",
                    features: ["info"]
                },
                bottomEnd: "paging"
            },
            scrollX: true,

            initComplete: function() {},
            rowCallback: function(row, data) {

                // Önce eski renklendirmeyi temizle
               $(row).removeClass("table-danger");

               // Checkbox işaretli değilse hiçbir şey yapma
               if (!$("#highlightZeroPrice").is(":checked"))
                   return;

               // Sıfır miktar kontrolü
               if (data.SIFIR_FIYAT_VAR == "E") {
                   $(row).addClass("table-danger");
               }
            }
        });

        $('#Table tbody').on('change', '.group-checkbox', function() {

            const groupName = $(this).data("group");
            const checked = $(this).is(":checked");

            table.rows().every(function() {
                const rowData = this.data();
                if (rowData.CARI_ADI === groupName) {
                    $(this.node()).find('.row-checkbox').prop('checked', checked);
                }
            });
        });

        // AJAX başlamadan önce
        table.on('preXhr.dt', function() {
            $("#tableLoading").show();
        });

        // AJAX bittikten sonra
        table.on('xhr.dt', function() {
            $("#tableLoading").hide();
        });

        table.on('processing.dt', function(e, settings, processing) {
            if (processing) {
                $("#tableLoading").show();
            } else {
                $("#tableLoading").hide();
            }
        });

        // Görsel sınıf düzeltmeleri
        setTimeout(() => {
            const adjustments = [{
                    selector: ".dt-buttons .btn",
                    classToRemove: "btn-secondary"
                },
                {
                    selector: ".dt-buttons.btn-group",
                    classToAdd: "mb-md-0 mb-6"
                },
                {
                    selector: ".dt-search .form-control",
                    classToRemove: "form-control-sm",
                    classToAdd: "ms-0"
                },
                {
                    selector: ".dt-search",
                    classToAdd: "mb-0 mb-md-6"
                },
                {
                    selector: ".dt-length .form-select",
                    classToRemove: "form-select-sm"
                },
                {
                    selector: ".dt-layout-end",
                    classToAdd: "gap-md-2 gap-0 mt-0"
                },
                {
                    selector: ".dt-layout-start",
                    classToAdd: "mt-0"
                },
                {
                    selector: ".dt-layout-table",
                    classToRemove: "row mt-2"
                },
                {
                    selector: ".dt-layout-full",
                    classToRemove: "col-md col-12",
                    classToAdd: "table-responsive"
                }
            ];
            adjustments.forEach(({
                selector,
                classToRemove,
                classToAdd
            }) => {
                document.querySelectorAll(selector).forEach(el => {
                    if (classToRemove) classToRemove.split(" ").forEach(cls => el.classList.remove(cls));
                    if (classToAdd) classToAdd.split(" ").forEach(cls => el.classList.add(cls));
                });
            });
        }, 100);

        function getSelectedDocumentNumbers() {
            const documentNumbers = [];

            table.rows().every(function() {
                const node = this.node();
                if ($(node).find('.row-checkbox').is(':checked')) {
                    documentNumbers.push(this.data().BELGE_NO);
                }
            });

            return documentNumbers;
        }

        function createDocument() {
            var selectedDocumentNumbers = getSelectedDocumentNumbers();
            if (selectedDocumentNumbers.length < 1) {
                alert("İrsaliye oluşturmak için bir sevkiyet seçiniz.");
                return;
            }

            $.ajax({
                url: "/Shipment/CreateDocument",
                type: "POST",
                contentType: "application/json",
                data: JSON.stringify({
                    documentNumbers: selectedDocumentNumbers
                }),
                success: function(response) {
                    if (res.success) {
                        showToast("İrsaliye başarıyla oluşturuldu. İrsaliye No: " + res.documentNo, "success");
                    } else {
                        showToast(res.errorMessage, "danger");
                    }
                },
                error: function(xhr) {
                    console.error(xhr);
                    alert("İrsaliye oluşturulurken bir hata oluştu");
                }
            });
        }

        $("#btnFilter").on("click", function() {
            table.ajax.reload();
        });

        $("#btnClearFilters").on("click", function() {
            $("#startDate").val("");
            $("#endDate").val("");
            $("#status").val("");
            $("#showCompleted ").prop("checked", false);
            $("#highlightZeroPrice  ").prop("checked", false);

            table.ajax.reload();
        });

        document.addEventListener("DOMContentLoaded", function () {

            const today = new Date();
            const firstDay = new Date(today.getFullYear(), today.getMonth(), 1);

            document.getElementById("startDate").value = formatDateLocal(firstDay);
            document.getElementById("endDate").value = formatDateLocal(today);

            function formatDateLocal(date) {
                const year = date.getFullYear();
                const month = String(date.getMonth() + 1).padStart(2, "0");
                const day = String(date.getDate()).padStart(2, "0");
                return `${year}-${month}-${day}`;
            }
        });

        function showToast(message, type = "success") {

            const toastEl = document.getElementById("globalToast");
            const toastMessage = document.getElementById("toastMessage");

            toastEl.classList.remove("bg-success", "bg-danger", "bg-warning", "bg-info");

            switch (type) {
                case "success":
                    toastEl.classList.add("bg-success");
                    break;
                case "danger":
                case "error":
                    toastEl.classList.add("bg-danger");
                    break;
                case "warning":
                    toastEl.classList.add("bg-warning");
                    break;
                default:
                    toastEl.classList.add("bg-info");
                    break;
            }

            toastMessage.innerText = message;

            const bsToast = new bootstrap.Toast(toastEl, {
                delay: 3000
            });
            bsToast.show();
        }
    </script>
}

<div class="position-fixed bottom-0 end-0 p-3" style="z-index:1080;">
    <div id="globalToast" class="toast border-0 text-white" role="alert">
        <div class="d-flex">
            <div id="toastMessage" class="toast-body">
                <!-- JS ile doldurulacak -->
            </div>
            <button type="button" class="btn-close btn-close-white m-auto me-2" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    </div>
</div>
